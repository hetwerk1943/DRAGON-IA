apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-orchestrator
  namespace: dragon-ia
  labels:
    app: ai-orchestrator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-orchestrator
  template:
    metadata:
      labels:
        app: ai-orchestrator
    spec:
      containers:
        - name: ai-orchestrator
          image: dragon-ia/ai-orchestrator:latest
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: ai-orchestrator
  namespace: dragon-ia
spec:
  selector:
    app: ai-orchestrator
  ports:
    - port: 8000
      targetPort: 8000
