# patches/
This directory stores generated diff/patch files produced by the Repo Agent.

Each file follows the naming convention:
  `YYYY-MM-DD_HH-mm_shortDescription.patch`

Patch files are created automatically by the Agent panel (`agent.html`)
and can be reviewed and applied to the repository.
